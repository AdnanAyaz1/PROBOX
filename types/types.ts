export interface Product {
  id: number;
  name: string;
  price: number;
  img: string;
}
export interface CartItem {
  id: number;
  name: string;
  price: number;
  img: string;
  quantity: number;
}